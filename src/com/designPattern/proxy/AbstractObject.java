package com.designPattern.proxy;

/**
 * declared the common interface of original object and target object, then we
 * can use proxy object anywhere we use original object
 * 
 * @author DeBen
 *
 */
public abstract class AbstractObject {
	public abstract void operation();
}
